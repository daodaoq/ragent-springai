# P8: RAG 知识库项目审查报告

> 状态：审查完成 2026-08-05；**P0 全部 13 项 ✅**；**P1 完成 8/8**（5b/6a/6b/6c/7a/7b ✅，**5a ✅ 本轮完成**，5c 用户决策暂缓）；**P2 完成 7/7**（8a-8g，部分项见说明）
> 迁移脚本已全部执行（p8_trace_id / p8_eval / p8_source / **p9_async_ingest / p9_multi_kb**）；FULLTEXT 索引已确认存在
> 审查方式：5 路并行只读审计（摄取 / 检索 / 生成·记忆·模型治理 / 评测·可观测性 / 安全·数据·基础设施）
> 说明：每一项带 `[ ]` 勾选，修复完成后改为 `[x]` 并附修复说明。
>
> **本轮新增迁移脚本**（对已存在的库手动执行一次）：
> - `backend/sql/p8_trace_id.sql`：`rag_query_log`、`ai_feedback` 补 `trace_id` 列
> - `backend/sql/p8_eval.sql`：新建 `eval_result` 评测结果表
> - **`backend/sql/p9_async_ingest.sql`**：新建 `ingest_task` 异步任务表 + `kb_document.error_msg`
> - **`backend/sql/p9_multi_kb.sql`**：新建 `kb` 知识库表 + `kb_document.kb_id`（回填默认库）+ `rag_query_log.kb_id`

---

## 总体评价

RAG 主链路完成度高：结构感知切分 + 溯源元数据、7 阶段查询管线（共享一次 LLM 提取）、混合检索 + 重排完整降级链、三态熔断 / Redis 限流 / Lua 原子、ELK 日志体系。

作为可上线、可信赖的 RAG 系统，短板集中在四块：**幻觉防护、数据一致性与权限隔离、评测反馈闭环、工程化（测试 / 可观测性）**。

---

## P0 — 直接影响正确性 / 安全性 / 稳定性

### P0-1 幻觉防线薄弱

- [x] 1a. 检索无相关性阈值：低相关 chunk 照常进 prompt；空结果仍触发生成。→ **已修**：`ragent.retrieval.min-score`（默认 0.3，仅 rerank 成功路径生效）+ `ragStream` 空召回硬门禁直接返回提示语、不再调生成模型（`RetrievalServiceImpl.retrieve`、`RagServiceImpl.ragStream`）
- [x] 1b. 引用 `[N]` 无校验：模型可编造超出文档数的编号。→ **已修**：prompt 显式给出 `[1]~[N]` 可引用范围 + 系统提示词禁止编造编号 + 生成后 `validateCitations` 越界告警留痕（`RagServiceImpl`）
- [x] 1c. 实体误抽 filename 致零召回且不降级：硬过滤让双路全空，空结果不重试。→ **已修**：实体过滤下零召回自动去掉 filter 重试一次（`RetrievalServiceImpl.retrieve`）
- [x] 1d. 提示词注入：文档 / 历史原样拼 prompt，无边界符与"不可信数据"声明。→ **已修**：`buildPrompt` 每条来源加 `【来源 N】…【/来源 N】` 分界符 + 系统提示词声明知识库内容为不可信数据、忽略其中指令；`StructuredExtractor` 同步加固（`RagServiceImpl`、`StructuredExtractor`）

### P0-2 数据一致性 MySQL ↔ Qdrant

- [x] 2a. 稠密检索不过滤 status/deleted，孤儿向量可被检索到。→ **已修**：检索尾部按 `documentId` 一次性反查 `kb_document`（`@TableLogic` 自动带 deleted=0 + status='READY'），丢弃孤儿向量切片，拉齐两路行为（`RetrievalServiceImpl.filterActiveDocuments`）
- [x] 2b. 删除时 Qdrant 失败被忽略，MySQL 已删而向量残留。→ **已修**：`cleanupChunksAndVectors` Qdrant 删除失败直接抛错（失败补偿路径外层 try/catch 已兜底），删除/重切/重试失败可感知可重试（`KnowledgeBaseServiceImpl`）

### P0-3 数据隔离与越权

- [x] 3a. 任意登录用户可删 / 重切 / 批量删全库文档。→ **已修**：`rechunk / retry / delete / batch-delete` 改为 `@SaCheckRole(value={"ADMIN","TEACHER"}, mode=OR)`（`KnowledgeBaseController`）
- [x] 3b. 旧 SSE 端点绕过 Redis 限流，匿名可检索全库。→ **已修**：`/ai/chat|rag|agent/stream` 通过 `rateLimited` helper 接入与 `/ai/stream` 同一全局队列限流，未获执行权回退"繁忙"提示（`AiChatController`）
- [x] 3c. 会话记忆跨用户串话：key 无 userId，空 conversationId 共享 default。→ **已修**：新增 `RagentContext.userScope()`（登录→`u{userId}`，匿名→`anon`），`ChatMemoryServiceImpl` 与 `MemorySummaryServiceImpl` 的存储键都加用户作用域前缀（`ChatMemoryServiceImpl.key`、`MemorySummaryServiceImpl.scopedConvId`）。注：升级后旧无作用域键自然过期；登录态变化会得到全新记忆

### P0-4 稳定性与线程模型

- [x] 4a. Netty 事件循环被阻塞：admitted 路径同步跑意图 LLM + 检索 + rerank。→ **已修**：`/ai/stream` admitted/queued 与旧端点全部改为 `Flux.defer(engine).subscribeOn(boundedElastic)`，阻塞管线移出事件循环（`UnifiedChatServiceImpl`、`AiChatController.rateLimited`）
- [x] 4b. 同步 `.call()` 无超时：Agent / 改写 / 摘要 / 评测可能挂死占线程。→ **已修**：`ChatModelRouter.call` 用有界池 + `Future.get(syncTimeoutMs)`（默认 30s），超时抛错进入候选降级；新增 `ragent.model.sync-timeout-ms` 配置（`ChatModelRouter`、`ModelFailoverProperties`）
- [x] 4c. 限流 inFlight 误减 + 断连泄漏。→ **已修**：`RELEASE_ADVANCE_LUA` 改状态感知——仅 admitted 才 DECR，queued/rejected/缺失只摘出 ZSET 不 DECR；请求键 TTL 60s→600s 杜绝长任务 release 漏判（`RedisRateLimiter`）
- [x] 4d. 熔断器 fallback 成功把失败端点 recordSuccess。→ **已修**：`tryStream` 的 `doOnComplete(recordSuccess)` 移到 `onErrorResume` 之前，成功只记在真正成功的端点链上（`ChatModelRouter`）

---

## P1 — 迭代能力 / 扩展性

- [x] 5a. 摄取全同步阻塞，无任务队列 / DLQ / 自动重试（`KnowledgeBaseServiceImpl`）。→ **已修（P9）**：新建 `ingest_task` 任务表（QUEUED/RUNNING/SUCCESS/FAILED/DLQ/CANCELLED），上传/重切/重试只做"校验+落 MinIO+入队"立即返回（批量不再 `future.get()` 阻塞 HTTP）；`IngestTaskServiceImpl` 轮询 worker（CAS claim + 每文件名条带锁串行化两阶段替换）消费，`SYSTEM_ERROR`(500) 瞬时失败自动重试（attempt++ 回 QUEUED、文档回 PENDING），`BAD_REQUEST`(400) 永久失败或超限直接 DLQ + 文档 FAILED + `error_msg` 回填；`/kb/tasks` 运维看队列/DLQ；前端上传后轮询文档状态、PROCESSING「处理中」徽章。注意：`process()` 错误码保留（400 不再被包成 500），避免坏文件烧满重试次数
- [x] 5b. `fileHash` 空转，内容去重 / 变更检测未实现（`KnowledgeBaseServiceImpl`）。→ **已修**：同名同内容且 READY 直接返回跳过处理；并改为**两阶段替换**——新文档处理成功后才删旧文档，杜绝"先删后处理失败丢数据"
- [ ] 5c. 无 OCR，扫描件整文档 FAILED——**用户决策暂缓（2026-08-05 P9）**：需引入 OCR 引擎。已评估两条路径——① DashScope 云端 OCR（复用现有 DASHSCOPE_API_KEY，仿 `DashScopeRerankClientImpl` 加 HTTP 客户端，零新增基础设施）；② 本地 Tesseract（需宿主安装 + 中文语言包，应用未容器化，部署成本高）。扫描件现仍走 FAILED + `error_msg` 提示（异步队列中 400 直接 DLQ 不烧重试）
- [x] 6a. 反馈不回流评测，无子维度 / traceId 关联。→ **已修（traceId 部分）**：`ai_feedback` 加 `trace_id`，提交时优先用 DTO 显式值，否则按"会话+问题"从查询日志回填；前端反馈列表展示 traceId。子维度打分与"回流评测集"留待后续
- [x] 6b. 评测纯内存不持久，无定时 / CI 回归（`EvalServiceImpl`）。→ **已修**：新增 `eval_result` 表 + `EvalResult` 实体/Mapper，每次 `/eval/run` 自动持久化汇总指标 + 完整报告 JSON；`GET /eval/history` 返回历史（前端评测 Tab 展示最近 10 次趋势）
- [x] 6c. 评测不严谨（`EvalServiceImpl`）。→ **已修**：裁判低温度 0.1 + 解析失败自动重试 1 次（不再静默记 0）；**评测注入不再污染生产 KB**——`kb_document.source`（UPLOAD/EVAL，迁移 `sql/p8_source.sql`），生产检索与知识库列表排除 EVAL，评测程序用 `includeEval=true` 召回（`RetrievalQuery`/`RagService.retrieve` 加 includeEval 参数、`KnowledgeBaseService.markSource`）。**未做**：RAGAS context 指标（需 LLM 逐 chunk 相关性判定）、独立裁判模型（单提供商 DeepSeek，仅降温度）
- [x] 7a. 无任何缓存：重复查询每次 2 LLM + rerank + ≤9 次检索。→ **已修**：检索结果短 TTL 内存缓存（`ragent.retrieval.cache-ttl-seconds=60`），键=查询+过滤+topK；文档上传完成/删除/重切时 `RetrievalService.invalidateCache()` 主动失效（`RetrievalServiceImpl`、`KnowledgeBaseServiceImpl`）
- [x] 7b. 追踪断点：`rag_query_log` 无 traceId，无法与 ELK 关联。→ **已修**：`rag_query_log` 加 `trace_id` 列（迁移 `sql/p8_trace_id.sql`），RagService 落库时取 `RagentContext.traceId`；ES 日志页与查询日志页均展示 traceId，坏案例可反查请求链路

---

## P2 — 工程化 / 运维 / 体验

- [x] 8a. 零测试（无任何 `*Test.java` / `*.test.*`）。→ **已修（最小集）**：`ragent-ai` 加 `spring-boot-starter-test`(test) + 13 个单测——熔断器三态、查询规范化、RRF/实体降级/重排空回退（Mockito 桩）、结构切分；顺带修了 `QueryNormalizer` 两处口头禅剥离缺陷。运行：`mvn -pl ragent-ai -am test`
- [x] 8b. 无系统级指标 / 告警 / 成本。→ **已修（指标部分）**：`ragent-web` 加 actuator + `micrometer-registry-prometheus`，暴露 `/api/actuator/prometheus`；自定义指标 `ragent_ratelimit_{admitted,queued,rejected}_total`、`ragent_ratelimit_{inflight,queued}`、`ragent_rag_query_latency_*`（按 intent 分桶）。**未做**：TokenUsage 成本采集、Prometheus 部署与告警规则
- [x] 8c. 权限模型粗放。→ **已修**：自注册一律 STUDENT（教师/管理员由管理员授予，`RegisterDTO` 去 role）；`GET /kb/documents` 改登录可见；token 加 `active-timeout` 滑动续期；登录失败 5 次锁 15 分钟（Redis）；知识库上传扩展名白名单
- [x] 8d. 密钥硬编码 fallback + 弱默认凭据 + 中间件匿名暴露。→ **已修（文档/约束层）**：新增 `.env.example` + `docs/P9-部署与运维.md` 安全清单。**未改**：`application-local.yml` 的 Key 默认值与中间件弱凭据未删除（避免破坏当前运行；按文档用环境变量覆盖 + 网络 ACL 收敛端口）
- [x] 8e. 检索健壮性。→ **已修（部分）**：rerank 返回空列表回退 RRF 融合结果；启动自检 FULLTEXT 索引缺失打醒目标记 WARN（`FulltextIndexCheck`）；**多知识库 ✅（P9）**——`kb` 表 + `kb_document.kb_id`，聊天页 KB 下拉（默认全部库），kbId 贯穿 `ChatRequest → UnifiedChatService → RagService.ragStream → RetrievalQuery → 检索`；正确性靠 DB 后置过滤（`filterActiveDocuments` 按 `kb_id` 收窄 + 关键词 SQL `d.kb_id` 谓词 + 检索缓存键含 kbId），稠密 payload 预过滤经 `ragent.retrieval.dense-filter-by-kbid` 开关（默认关，兼容旧向量）。**未做**：标签/日期过滤、ngram 停用词
- [x] 8f. 运维。→ **已修（部分）**：docker-compose 全服务加 `mem_limit`/`cpus` 资源限制 + qdrant/minio 补健康检查；新增 `scripts/backup.sh`（mysqldump）与 `docs/P9-部署与运维.md`。**未做**：HA/主从、应用 Dockerfile/容器化
- [x] 8g. 文档沉淀。→ **已修**：新增 `CLAUDE.md` + `docs/P9-架构文档.md`（P6/P7/P8 架构沉淀）+ `docs/P9-部署与运维.md`

---

## 数据遗留问题（2026-08-05 核实并已清理）

**早期误判修正**：曾按"同名计数"（含逻辑删除行）判定存在 129 个重复文档。核实后确认——**活跃数据（deleted=0）本就没有同名重复**，那些"重复"是**逻辑删除的历史残留**（重复上传/删除时旧文档被应用置 deleted=1，`@TableLogic` 下不可见），属正常累积而非数据损坏。
- 已**实际执行清理**（先备份 `backups/ragent_before_dedup.sql`）：物理删除 135 条 deleted=1 的死行。
- 清理后三方完全一致：**46 文档 / 869 切片 / Qdrant 869 向量，0 同名重复、0 孤儿**。
- 代码侧已**加固**：`findLatestByFilename` 用 `selectOne + LIMIT 1` 取最新一条，即便将来出现同名多行，`selectOne` 也不会抛 `TooManyResultsException` 导致上传失败。
- 清理脚本 `backend/sql/p8_dedup.sql` 已更新为幂等的"清逻辑删除残留"版本。

---

## P9 新增（2026-08-05：5a 异步摄取 + 多知识库）

### 异步摄取任务队列（5a）
- 上传/重切/重试 → `enqueue*`：校验 + 落 MinIO + 写 `ingest_task`（QUEUED）秒回，解析/切分/向量化由 worker 消费。`processDocument(documentId, taskType)` 承载实际处理：UPLOAD 两阶段替换（READY 后删旧同名文档）、RECHUNK/RETRY 先清旧切片/向量。
- **错误分类**：`process()` 保留原始错误码——`BAD_REQUEST`(400)=永久失败直接 DLQ；`SYSTEM_ERROR`(500)=瞬时，attempt++ 回 QUEUED 自动重试，超 `max_attempts` 进 DLQ + 文档 FAILED + `error_msg`。
- **并发安全**：CAS claim（`UPDATE ... WHERE status='QUEUED'`）防多轮询重复消费；每文件名 64 条带 ReentrantLock 串行化同文件名两阶段替换/重切/重试/删除；`delete()` 先 `cancelQueuedByDocument` 再清理。
- 前端：上传秒回（进度条改"已提交 N/M"），文档 PENDING/PROCESSING 时每 3s 轮询；PROCESSING 蓝色「处理中」徽章、FAILED 展示 error_msg。

### 多知识库（共享池 + 分级管理，用户已决策）
- 模型：所有登录用户可见全部库并可检索；仅 TEACHER/ADMIN 创建/管理；学生只读。`kb.owner_id` 仅记录，无成员表。
- **kbId 不贯穿 QueryPipeline**（QueryContext/各 stage 不动），作为独立参数 `stream(..., kbId) → ragStream(..., kbId) → toRetrievalQuery(pq, kbId)`。
- **正确性靠 DB 后置过滤**（旧 Qdrant payload 无 kbId）：`filterActiveDocuments` 加 `kb_id` 谓词 + 关键词 SQL `d.kb_id = ?` + 检索缓存键含 kbId；稠密 payload 预过滤走 `dense-filter-by-kbid` 开关（默认关，全量重摄入后可开）。**实体过滤零召回重试必须保留 kbId**（否则指定库检索重试后泄漏跨库结果）。
- 端点：`GET /kb/list`、`POST /kb`、`PUT /kb/{id}`、`DELETE /kb/{id}`（默认库/非空拒）；`GET /kb/documents?kbId=`、上传 `kbId` 参数、`/ai/stream` body 加 `kbId`。
- 前端：聊天页 KB 下拉（默认全部库）+ 知识库管理页按库过滤/新建/删除 + 文档表「知识库」列。

### 既有数据兼容
- 迁移 `p9_multi_kb.sql` 创建默认库（id=1）并把现有 `kb_document.kb_id` 回填为 1；旧 Qdrant 向量无 kbId payload，靠 DB 后置过滤兼容（检索「全部库」或「默认库」均正确）。
- 检索缓存全局失效（文档增删改/重切），KB 删除非空即拒无级联 → 无跨库缓存泄漏。

---
## 已有亮点（保留）

- 结构感知分片 + 行号 / 字符偏移 / 章节路径溯源元数据
- 7 阶段管线共享一次结构化提取，LLM 调用压到 2 次/查询，意图门禁避免非 RAG 白检索
- rerank / 关键词 / 意图三级降级链完整
- Lua+ZSET+Pub/Sub 限流原子性、三态熔断、MDC/traceId 经 TTL 线程池透传
- ELK 双通道日志、查询日志自动采集

---

## 建议改进路线图（按投入产出比）

| 优先级 | 动作 | 约耗时 |
|---|---|---|
| 立刻 | 幻觉防护：分数阈值 + 空结果拒答 + 引用校验；稠密检索补状态过滤 + 删除对账 | 0.5–1 天 |
| 立刻 | 稳定性：engine 链路 `subscribeOn(boundedElastic)`；同步 `.call()` 加超时；修熔断 recordSuccess 错位；修限流计数 / 泄漏 | 1 天 |
| 本周 | 隔离：记忆 key 加 userId、空 conversationId 兜底；知识库操作加角色权限；旧 SSE 端点收敛 / 补限流；上传加类型白名单 | 1–2 天 |
| 本周 | 摄取：异步任务表 + 自动重试；fileHash 内容级幂等（先校验后删） | 1–2 天 |
| 2 周 | 评测闭环：eval 结果表 + 定时 / CI 回归、RAGAS context 指标、反馈关联 traceId 回流 | 2–3 天 |
| 2 周+ | 工程化：最小单测 / 集成测试、Micrometer + Prometheus、多知识库模型 | 持续 |
